---
title: ResourceProjectSchema
sidebar_position: 1
---

[frontend](../../..) • Docs

***

[frontend](../../..) / [types/resource.type](..) / ResourceProjectSchema

# Variable: ResourceProjectSchema

> `const` **ResourceProjectSchema**: `ZodObject`\<`object`, `"strip"`, `ZodTypeAny`, `object`, `object`\>

For projects implementation we use a wrapper

## Type declaration

### addedBy

> **addedBy**: `ZodString`

### resource

> **resource**: `ZodObject`\<`object` & `object`, `"strip"`, `ZodTypeAny`, `object`, `object`\> = `ResourceSchema`

## Source

src/types/resource.type.ts:86
